<nav class="bg-charcoal border-b-4 border-mustard shadow-lg">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <div class="flex items-center">
        <%= link_to root_path, class: "text-xl font-bold text-white hover:text-mustard transition" do %>
          Co-Create
        <% end %>
      </div>

      <div class="flex items-center gap-4">
        <% if authenticated? %>
          <%= link_to "Projects", projects_path, class: "text-sm font-medium text-charcoal-200 hover:text-mustard transition" %>
          <%= button_to "Log out", session_path, method: :delete, class: "text-sm font-medium text-charcoal-200 hover:text-mustard transition" %>
        <% else %>
          <%= link_to "Sign in", new_session_path, class: "text-sm font-medium text-charcoal-200 hover:text-mustard transition" %>
          <%= link_to "Sign up", new_user_path, class: "text-sm font-medium px-4 py-2 bg-lavender text-white rounded-md hover:bg-lavender-500 transition" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
