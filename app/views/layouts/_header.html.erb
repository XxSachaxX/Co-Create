<nav class="fixed top-0 left-0 right-0 z-50 bg-white/80 backdrop-blur-md border-b border-charcoal-100 shadow-sm">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Logo -->
      <div class="flex items-center">
        <%= link_to (authenticated? ? projects_home_path : root_path), class: "group flex items-center gap-2" do %>
          <div class="w-8 h-8 rounded-lg bg-gradient-to-br from-lavender via-lavender-400 to-mustard flex items-center justify-center shadow-sm group-hover:shadow-md transition-all duration-200">
            <span class="text-white font-bold text-sm">C</span>
          </div>
          <span class="text-xl font-bold bg-gradient-to-r from-lavender to-lavender-700 bg-clip-text text-transparent group-hover:from-lavender-700 group-hover:to-mustard transition-all duration-300">
            <%= I18n.t('layouts.header.app_name') %>
          </span>
        <% end %>
      </div>

      <!-- Navigation -->
      <div class="flex items-center gap-3">
        <% if authenticated? && @user.present? %>
          <%= link_to I18n.t('layouts.header.find_project'), projects_home_path, class: "text-sm font-medium px-4 py-2 border border-lavender-200 text-lavender-700 rounded-lg hover:bg-lavender-50 hover:border-lavender-300 hover:shadow-sm transition-all duration-200" %>
          <%= link_to I18n.t('layouts.header.your_projects'), user_projects_path(@user), class: "text-sm font-medium px-4 py-2 border border-mustard text-mustard-500 rounded-lg hover:bg-mustard-50 hover:border-mustard-500 hover:shadow-sm transition-all duration-200" %>
          <%= button_to I18n.t('layouts.header.log_out'), session_path, method: :delete, class: "text-sm font-medium px-4 py-2 border border-charcoal-200 text-charcoal-600 rounded-lg hover:border-red-300 hover:text-red-600 hover:bg-red-50 transition-all duration-200" %>
        <% else %>
          <%= link_to I18n.t('layouts.header.sign_in'), new_session_path, class: "text-sm font-medium text-charcoal-500 hover:text-lavender-700 px-3 py-2 rounded-lg hover:bg-lavender-50 transition-all duration-200" %>
          <%= link_to I18n.t('layouts.header.sign_up'), new_user_path, class: "text-sm font-medium px-4 py-2 bg-gradient-to-r from-lavender to-lavender-400 text-white rounded-lg hover:from-lavender-700 hover:to-lavender-400 hover:shadow-md transition-all duration-200" %>
        <% end %>
      </div>
    </div>
  </div>

  <!-- Subtle gradient accent line -->
  <div class="absolute bottom-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-lavender-200 to-transparent"></div>
</nav>
