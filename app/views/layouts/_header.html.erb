<nav class="bg-white border-b border-gray-200 shadow-sm">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <div class="flex items-center">
        <%= link_to root_path, class: "text-xl font-bold text-gray-900 hover:text-primary transition" do %>
          Co-Create
        <% end %>
      </div>

      <div class="flex items-center gap-4">
        <% if authenticated? %>
          <%= link_to "Projects", projects_path, class: "text-sm font-medium text-gray-600 hover:text-gray-900 transition" %>
          <%= button_to "Log out", session_path, method: :delete, class: "text-sm font-medium text-gray-600 hover:text-gray-900 transition" %>
        <% else %>
          <%= link_to "Sign in", new_session_path, class: "text-sm font-medium text-gray-600 hover:text-gray-900 transition" %>
          <%= link_to "Sign up", new_user_path, class: "text-sm font-medium px-4 py-2 bg-primary text-black rounded-md hover:opacity-90 transition" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
